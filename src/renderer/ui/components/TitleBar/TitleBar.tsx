//Imports
import styles from "./TitleBar.module.css"

//Main
const TitleBar = () => {
  return (
    <header className={styles.titlebar}>
      <span className={styles.title}>Mai ImageFlow</span>
    </header>
  )
}

export default TitleBar
